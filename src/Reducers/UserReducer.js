
const INITIAL_STATE = {
  fname: '',
  lname: '',
  age: 0,
  zipcode: '',
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
      default:
        return state;
  }
};
